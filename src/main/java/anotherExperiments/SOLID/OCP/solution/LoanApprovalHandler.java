package anotherExperiments.SOLID.OCP.solution;

public class LoanApprovalHandler {

    public void approvalLoan(Validator validator) {
        if (validator.isValid()) {
            //to do something
        }
    }
}
