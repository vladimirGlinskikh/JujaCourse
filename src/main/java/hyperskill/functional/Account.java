package hyperskill.functional;

import java.util.List;

public class Account {
//    List<Account> nonEmptyAccounts =
//            filter(accounts, account -> account.balance > 0);
//    List<Account> accountsWithTooMuchMoney =
//            filter(accounts, account -> !account.isLocked() && account.balance >= 100_000_000);
}
